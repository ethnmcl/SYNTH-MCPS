# Future Roadmap

- Add first-class pagination cursors for large accounts.
- Add incident timeline generation from deploy + logs + metrics.
- Add cached resource snapshots for low-latency reasoning.
- Add provider capability probing at startup.
- Add signed approval workflows for destructive operations.
