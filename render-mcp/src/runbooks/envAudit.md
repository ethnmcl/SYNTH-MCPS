# Environment Audit

1. List env metadata using `list_env_var_metadata`.
2. Compare staging and production key sets.
3. Flag credential-like keys that are not secret.
4. Validate key naming consistency across services.
5. Review stale or orphaned key metadata monthly.
